# FFI Founder Copilot – Backend

FastAPI-Backend. Läuft lokal unter `http://127.0.0.1:8000`.

## Voraussetzungen

- Git installiert
- Python 3.9 oder höher

## 1. Projekt klonen

```bash
git clone https://github.com/Robin100804/ffi_founder_copilot.git
cd ffi_founder_copilot/backend

5. Datei **speichern** (⌘S / Ctrl+S).

---

## 3. README committen und pushen

Im Terminal (immer noch in `backend`):

```bash
git status          # hier solltest du sehen: README.md modified
git add README.md
git commit -m "Add README content"
git push